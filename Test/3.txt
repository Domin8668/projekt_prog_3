setlocal enabledelayedexpansion
@echo off

set suma=0
set biale=0
set inne=0

for /f "tokens=* delims=" %%i in (test.txt) do (
	for /f "tokens=* delims=" %%x in (%%i) do (
	set string=%%x
	if string==	  (!biale!=!biale!+1) else (!inne!=!inne!+1)
	
	)
)

set /a suma=!biale!+!inne!
if !suma! %%2==0 

pause
